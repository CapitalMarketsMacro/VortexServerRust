# Shell scripts must keep LF endings so they run on Linux/macOS.
*.sh text eol=lf

# Vendored static libraries (vendor/solclient/) are binary — never apply
# text/EOL conversion, which would corrupt the archives on a Windows /
# autocrlf checkout. (These are also the only place we ship .a/.o blobs.)
*.a  binary
*.o  binary
