target/
dist/
Vortex/dist/
conan_output/
Vortex/conan_output/
# Conan cmake_layout's default output dir (created when conan install is run
# without --output-folder, e.g. during manual lockfile regeneration). The
# real build uses --output-folder conan_output, so this never appears there.
Vortex/crates/perspective-server/build/

# Local runtime config and logs
config.json
logs/
.cache
.clangd
.DS_Store
.idea/
*.so
*~

# Simulator runtime artefacts — each simulator stages its own venv /
# node_modules on first run; we never commit them.
.venv/
__pycache__/
*.pyc
node_modules/
package-lock.json
*.iml
.idea/perspective-native.iml
